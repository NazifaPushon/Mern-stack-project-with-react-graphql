import { Container } from '@mui/material';
import React from 'react';
import MenuBar from '../components/MenuBar';

const Home = () => {
    return (
        <Container>
           <MenuBar/>
        </Container>
    );
};

export default Home;